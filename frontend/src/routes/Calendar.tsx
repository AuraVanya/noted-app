import { PagePlaceholder } from "@/components/PagePlaceholder";

export const Calendar = () => (
  <PagePlaceholder
    title="Calendar"
    description="Your week, synced from Google Calendar."
    phase="Phase 3"
  />
);
