import { PagePlaceholder } from "@/components/PagePlaceholder";

export const Tickets = () => (
  <PagePlaceholder
    title="Tickets"
    description="Your assigned Jira issues, as a status-column Kanban board."
    phase="Phase 5"
  />
);
