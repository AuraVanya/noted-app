import { PagePlaceholder } from "@/components/PagePlaceholder";

export const Home = () => (
  <PagePlaceholder
    title="Home"
    description="Greeting, today's meetings, and recent projects will live here."
    phase="Phase 3"
  />
);
